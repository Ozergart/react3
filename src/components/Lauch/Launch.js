import React from 'react';

const {flight_number,mission_name, launch_year, links} = launch;
let small_patch = links[mission_patch_small]
const Launch = ({launch}) => {
    return (
        <div>
<div></div>
<div></div>
<div></div>
        </div>
    );
};

export default Launch;